services:
  - type: web
    name: rootiq
    runtime: python
    rootDir: incident_app
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn rootiq.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false